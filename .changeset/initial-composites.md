---
"@hannature/ui": minor
---

Add composite components — MealCard, WorkoutCard, DayTile, StarCounter, RewardBanner — each with web (`.tsx`) and React Native (`.native.tsx`) implementations sharing a typed contract. Stories use `tags: ['autodocs']` and the Storybook `addon-a11y` reports zero violations on every story.
