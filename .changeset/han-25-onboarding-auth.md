---
"@hannature/shared-types": minor
---

Add `User`, `AuthSession`, `AuthTokens`, `LoginRequest`, `RegisterRequest`, `BehavioralGoal`, `LanguagePreference`, and `ConsentRecord` types plus the `CONSENT_VERSION` constant. These power the mobile onboarding & authentication flow (HAN-25) and will be reused by the API once consent endpoints land.
