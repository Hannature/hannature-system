# @hannature/ui

## 0.2.0

### Minor Changes

- ef01bc4: Add composite components — MealCard, WorkoutCard, DayTile, StarCounter, RewardBanner — each with web (`.tsx`) and React Native (`.native.tsx`) implementations sharing a typed contract. Stories use `tags: ['autodocs']` and the Storybook `addon-a11y` reports zero violations on every story.
